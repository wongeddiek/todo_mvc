<h1>Articles</h1>

<%= link_to 'New Article', new_article_path %> 


<%= render @articles %>
<!--
the above code does the same as below.  Rails automatically iterate the @articles array,
and render each article
-->

<!--
<% @articles.each do |article| %>
  <%= render article %>
<% end %>
-->

<!-- <table>
  <thead>
    <tr>
      <th>Title</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.user.email %></td>
        <td><%= link_to 'Show', article %></td>
        <td><%= link_to 'Edit', edit_article_path(article) %></td>
        <td><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

-->
